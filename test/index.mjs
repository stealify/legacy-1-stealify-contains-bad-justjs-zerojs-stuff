import { combine, map, runEffects, startWith, tap } from '../dist/core.mjs'
import { newDefaultScheduler } from '../dist/scheduler.mjs'
import { input } from '../dist/dom-event.mjs'



console.log(combine, map, runEffects, startWith, tap,input)