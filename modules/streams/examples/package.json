{
  "name": "stealify-examples",
  "version": "0.0.0-pre0",
  "private": true,
  "description": "stealify examples",
  "scripts": {
    "postinstall": "echo 'open the folder in a browser via file://<path> or serve it via http' && http-server"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "http-server": "^0.12.0",
    "stealify": "^0.0.6"
  }
}
